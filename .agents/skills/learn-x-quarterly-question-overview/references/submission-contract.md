# 提交契约

`apply` 的输入必须包含 `quarter`、`documentToken`、`revisionId`，以及 `changes`、`creates`、`events`、`unprocessed` 数组。修改议题或判断必须带同一文档中的证据锚点和对应认知事件；治理动作（优先级、研究状态、创建议题）不得伪造成认知事件。

禁止修改议题编号、生命周期 `状态` 或系统字段，禁止删除记录。新议题必须明确 `议题`、`类型`、`议题周期`；缺少优先级、研究状态时使用 `P1（默认）`、`继续研究（默认）`。
